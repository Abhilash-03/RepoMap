{
  "name": "repomap",
  "version": "1.0.0",
  "description": "GitHub Repository File Dependency Analyzer - Visual dependency graph and orphan file detection",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": ["github", "dependency", "analyzer", "visualization", "react-flow"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
